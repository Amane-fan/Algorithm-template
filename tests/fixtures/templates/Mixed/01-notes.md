# Markdown 排版验收

这是 **加粗**、*强调*、~~删除线~~、`inline_code` 和 [外部链接](https://typst.app) 的展示。

- 无序列表
  - 嵌套列表
- 第二项

1. 有序步骤
2. 下一个步骤

> 引用块：中文说明与英文 algorithm 可以一起展示。

| 算法 | 时间复杂度 | 空间 |
| :--- | :---: | ---: |
| 二分 | $O(\log n)$ | $O(1)$ |
| 排序 | $O(n\log n)$ | $O(n)$ |

行内公式 $a^2+b^2=c^2$。

$$
\sum_{i=1}^{n} i = \frac{n(n+1)}{2}
$$

![相对路径图片](assets/diagram.svg)

```cpp
// Markdown 内的代码块
int answer = 42;
```
